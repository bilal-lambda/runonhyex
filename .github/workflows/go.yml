name: Go

on:
  push:
    branches: [ "main" ]

jobs:

  build:
    runs-on: hyex
    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
