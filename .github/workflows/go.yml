name: Go

on:
  push:
    branches: [ "main" ]

jobs:

  build:
    runs-on: hyex
    steps:
    - uses: actions/checkout@v3

    - name: Check
      run: echo %GOROOT%

    - name: Cleanup
      run: set GOROOT=
    
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.19'
        cache: false

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
